AssetsServiceClient:
  ServiceUrl:
    settings-key: AssetsServiceUrl

  ExpirationPeriod:
    settings-key: ExpirationPeriod

HistoryExportBuilderJob:
    Db:
      LogsConnString:
        settings-key: LogsConnString
        types:
          - AzureTableStorage
      
      DataConnString:
        settings-key: DataConnString
        types:
          - AzureTableStorage
    
    GeneratedFileTtl:
      settings-key: HistoryExportFileTtl

MonitoringServiceClient:
  MonitoringServiceUrl:
    settings-key: MonitoringServiceUrl

OperationsHistoryServiceClient:
  ServiceUrl:
    settings-key: OperationsHistoryServiceUrl

SagasRabbitMq:
  RabbitConnectionString:
    settings-key: RabbitConnStr
    types:
      - RabbitMq
  
  RetryDelay:
    settings-key: RetryDelay
SlackNotifications:
  AzureQueue:
    ConnectionString:
      settings-key: SlackNotificationsConnString
      types:
        - AzureTableStorage
    
    QueueName: slack-notifications
  
  ThrottlingLimitSeconds:
    settings-key: ThrottlingLimitSeconds
